# Kokoro TTS Configuration (Local, Free TTS Engine)
# Install: pip install kokoro-onnx
# Docs: https://github.com/remsky/Kokoro-FastAPI

# Optional: Path to kokoro-onnx executable (defaults to "kokoro-onnx")
# KOKORO_PATH=kokoro-onnx

# Note: Kokoro is free and runs locally - no API key needed!
